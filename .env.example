# Environment Variables for 10xCards

# =============================================================================
# Application
# =============================================================================

# Application environment (development, staging, production)
NODE_ENV=development

# Public URL of the application (used for redirects, API calls)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# =============================================================================
# Supabase Configuration
# =============================================================================

# Supabase project URL (found in project settings)
# Example: https://xxxxxxxxxxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url_here

# Supabase anonymous key (safe to expose in client-side code)
# Used for client-side authentication and database access
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Supabase service role key (KEEP SECRET - server-side only)
# Has elevated permissions, never expose to client
# Used for admin operations and bypassing RLS
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# =============================================================================
# OpenRouter AI Configuration
# =============================================================================

# OpenRouter API key for AI model access
# Get your key from: https://openrouter.ai/keys
OPENROUTER_API_KEY=your_openrouter_api_key_here

# OpenRouter model to use (default: gpt-4o-mini)
# See available models at: https://openrouter.ai/models
OPENROUTER_MODEL=openai/gpt-4o-mini

# Optional: Your site URL for OpenRouter attribution
OPENROUTER_SITE_URL=https://your-site.com

# Optional: Your site name for OpenRouter attribution
OPENROUTER_SITE_NAME=10xCards

# =============================================================================
# Rate Limiting (Optional)
# =============================================================================

# Maximum number of AI generation requests per user per hour
RATE_LIMIT_AI_GENERATIONS=10

# Maximum number of flashcards to generate per request
MAX_FLASHCARDS_PER_REQUEST=10

# =============================================================================
# Development
# =============================================================================

# Enable verbose logging in development
DEBUG=false

# Disable Next.js telemetry
NEXT_TELEMETRY_DISABLED=1
